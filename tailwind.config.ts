/**
 * Tailwind CSS v4 Configuration
 *
 * Tailwind v4 is CSS-first. The primary configuration lives in app/globals.css
 * using the @import "@empoweredvote/ev-ui/tailwind-preset" CSS import path.
 *
 * This file is retained for IDE tooling compatibility.
 * ev-ui design tokens are applied via the CSS @import in globals.css.
 *
 * ev-ui brand colors (available via CSS custom properties):
 *   ev-coral:       #FF5740
 *   ev-muted-blue:  #00657C  (Empowered Listening primary)
 *   ev-light-blue:  #59B0C4
 *   ev-yellow:      #FED12E
 *
 * Font: Manrope (Google Fonts, loaded in app/layout.tsx via next/font)
 */
export default {};
